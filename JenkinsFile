pipeline {
    agent any
    
    triggers {
        githubPush()
      }
    stages {
        stage('Hello') {
            steps {
                echo "JENKINS URL IS %env.JENKINS_URL%"
                echo "BUILD_ID IS %env.BUILD_ID%"
            }
        }
    }
}
